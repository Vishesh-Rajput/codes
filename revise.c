#include <stdio.h>
int sum (int x,int y){
    int z= x+y;
    return z ;
}
int main()
 {
printf("opening user file\n");
char file[30];
*Ptr = &file;
 ptr=foropen("myfile.txt","r");
 fprintf(ptr, "%s",file);
     return 0;
}
